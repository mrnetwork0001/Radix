// Tailwind is pinned to v3 - v4 moves the PostCSS entry point to
// `@tailwindcss/postcss` and changes the config format entirely.
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
